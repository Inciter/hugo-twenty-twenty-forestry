---
label: Social Icons
hide_body: true
fields:
- name: networks
  type: field_group_list
  fields:
  - name: network_name
    type: text
    config:
      required: false
    label: Network Name
  - name: network_url
    type: text
    config:
      required: false
    label: Network URL
  - name: network_icon
    type: file
    config:
      maxSize: 64
    label: Network Icon
  config:
    min: 
    max: 
    labelField: 
  label: Networks
